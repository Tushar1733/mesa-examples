name: Validate Mesa Examples

on:
  push:
    paths-ignore:
      - "example_validation_results*.json"
      - "health_dashboard.md"
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:

  validate-using-latest-deps:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Disable GUI backend
        run: echo "MPLBACKEND=Agg" >> $GITHUB_ENV

      - name: Run validator
        run: xvfb-run -a python scripts/latest_validate_examples.py

      - name: Upload validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-report-latest
          path: "example_validation_results(latest-deps).json"
          if-no-files-found: warn

      # FIX: pull --rebase before push to avoid race condition with the
      # parallel validate-using-declared-deps job committing to the same branch
      - name: Commit validation report
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "example_validation_results(latest-deps).json"
          git diff --staged --quiet || (
            git pull --rebase origin ${{ github.ref_name }} &&
            git commit -m "ci: update validation report [skip ci]" &&
            git push
          )


  validate-using-declared-deps:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb
          sudo apt-get install -y lsof

      - name: Install script dependencies
        run: pip install requests pyyaml solara

      - name: Disable GUI backend
        run: echo "MPLBACKEND=Agg" >> $GITHUB_ENV

      - name: Clean stale venvs
        run: find examples -type d -name ".venv" -exec rm -rf {} +

      - name: Run validator
        env:
          MESA_VERSION_LABEL: "ci"
        run: python scripts/declared_validate_examples.py --examples-dir examples --timeout 30

      - name: Upload validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-report-declared
          path: "example_validation_results(declared-deps).json"
          if-no-files-found: warn

      # FIX: pull --rebase before push (same race condition fix as above)
      - name: Commit validation report
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "example_validation_results(declared-deps).json"
          git diff --staged --quiet || (
            git pull --rebase origin ${{ github.ref_name }} &&
            git commit -m "ci: update validation report [skip ci]" &&
            git push
          )


  check-examples:
    needs: [validate-using-latest-deps, validate-using-declared-deps]
    # FIX: always run this job even if a validator job fails, so the pipeline
    # is never skipped due to a validator error. Without this, a failed
    # validator marks its job as failed, and downstream jobs are skipped by default.
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: 'pip'

      - name: Install dependencies
        run: pip install requests pyyaml

      # FIX: download artifacts produced by the validator jobs instead of
      # relying on a git checkout that may not yet contain the freshly pushed
      # JSON files (race between push and this job's checkout).
      - name: Download latest-deps report
        uses: actions/download-artifact@v4
        with:
          name: validation-report-latest
        continue-on-error: true

      - name: Download declared-deps report
        uses: actions/download-artifact@v4
        with:
          name: validation-report-declared
        continue-on-error: true

      - name: Declare status
        run: python scripts/declare_status.py

      - name: Generate issue
        run: python scripts/generate_issues.py

      - name: Create or update issues
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            const issues = JSON.parse(
              fs.readFileSync('issues_to_create.json', 'utf8')
            );

            const existingIssues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open",
              per_page: 100
            });

            for (const issue of issues) {

              const existing = existingIssues.data.find(
                i => i.title === issue.title
              );

              if (existing) {

                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: existing.number,
                  labels: [
                    "example-health",
                    issue.status
                  ]
                });

              } else {

                await github.rest.issues.create({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  title: issue.title,
                  body: issue.body,
                  labels: [
                    "example-health",
                    issue.status
                  ]
                });

              }
            }


  update-dashboard:
    needs: [check-examples]
    # FIX: same as check-examples — run even if check-examples fails so the
    # dashboard always reflects the latest state rather than silently going stale.
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # FIX: pull latest commits (including JSON files pushed by validators)
      # so generate_dashboard_1.py always reads up-to-date data.
      - name: Pull latest changes
        run: git pull --rebase origin ${{ github.ref_name }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate dashboard
        run: python scripts/generate_dashboard_1.py

      - name: Commit dashboard
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add health_dashboard.md
          git diff --staged --quiet || git commit -m "ci: update health dashboard [skip ci]"
          git push